---
title: "New York Never Sleeps"
subtitle: "Як тисячі поїздок формують пульс міста"
author: "Genesis Team: Марія Носулько, Тетяна Ланіна, Марина Бондар"
output: 
  html_document
date: "`r Sys.Date()`"
execute:
  echo: false
  warning: false  
  message: false
  eval: true
---
## Вступ

У цьому проєкті команда Genesis дослідила дані New York City Taxi & Limousine Commission за вересень 2023 року, поєднавши їх із погодними спостереженнями Open-Meteo API.
Мета аналізу — зрозуміти, як змінюється поведінка міста:
  - де і коли відбувається найбільше поїздок,

  - як на це впливають погодні умови,

  - і як просторові особливості — густина населення, тип району — формують транспортний ритм Нью-Йорка.

**🗺️ Карта поїздок у дощові та ясні дні**

Перший крок нашого аналізу — просторове порівняння активності таксі в різних районах Нью-Йорка.
Для цього було створено дві синхронні карти, що показують кількість поїздок під час ясних і дощових днів у вересні 2023 року.

```{r}
library(arrow)
library(dplyr)
library(lubridate)
library(sf)
library(leaflet)
library(jsonlite)
library(leafsync)
library(ggplot2)
library(tidyverse)
library(httr)
library(leafsync)
library(scales)
library(ggspatial)
library(prettymapr)
library(janitor)


taxi <- read_parquet("/Users/ffff/Desktop/Project/yellow_tripdata_2023-09.parquet")

taxi <- taxi |> 
  mutate(
    pickup_datetime = ymd_hms(tpep_pickup_datetime),
    dropoff_datetime = ymd_hms(tpep_dropoff_datetime),
    trip_duration = as.numeric(difftime(dropoff_datetime, pickup_datetime, units = "mins")),
    date = as.Date(pickup_datetime)
  ) |> 
  select(PULocationID, trip_duration, date)


zones <- read.csv("/Users/ffff/Desktop/Project/taxi_zone_lookup.csv")

taxi <- taxi |> 
  left_join(zones |>  select(LocationID, Borough), by = c("PULocationID" = "LocationID")) |> 
  rename(borough = Borough)

url <- "https://archive-api.open-meteo.com/v1/archive?latitude=40.7128&longitude=-74.0060&start_date=2023-09-01&end_date=2023-09-30&daily=precipitation_sum,temperature_2m_max,temperature_2m_min&timezone=America/New_York"

weather_raw <- fromJSON(url)

weather <- data.frame(
  date = as.Date(weather_raw$daily$time),
  precipitation = weather_raw$daily$precipitation_sum,
  temp_max = weather_raw$daily$temperature_2m_max,
  temp_min = weather_raw$daily$temperature_2m_min
) |> 
  mutate(is_rainy_day = precipitation > 1)


merged_data <- taxi |> 
  left_join(weather, by = "date") |> 
  filter(!is.na(borough), trip_duration > 0, trip_duration < 120)


duration_map <- merged_data |> 
  group_by(borough, is_rainy_day) |> 
  summarise(avg_duration = mean(trip_duration, na.rm = TRUE)) |> 
  ungroup()

borough_shapes <- st_read("/Users/ffff/Desktop/Project/nybb_25c/nybb.shp")|> 
  rename(borough = BoroName)|>
  st_transform(crs = 4326)  


map_data <- borough_shapes |> 
  left_join(duration_map, by = "borough")



borough_trips <- merged_data |>
  group_by(borough, is_rainy_day) |>
  summarise(total_trips = n(), .groups = "drop")


map_data_trips <- borough_shapes |>
  left_join(borough_trips, by = "borough")


pal_trips <- colorNumeric(
  palette = "YlGnBu",         
  domain = map_data_trips$total_trips
)


rainy_map <- leaflet(map_data_trips |> filter(is_rainy_day == TRUE)) |>
  addProviderTiles("CartoDB.Positron") |>
  addPolygons(
    fillColor = ~pal_trips(total_trips),
    color = "white",
    weight = 1,
    fillOpacity = 0.85,
    popup = ~paste0(
      "<b>", borough, "</b><br>",
      "Rainy Day<br>",
      "Total Trips: ", formatC(total_trips, format = "d", big.mark = ",")
    )
  ) |>
  addLegend("bottomright", pal = pal_trips, values = ~total_trips,
            title = "Total Trips (Rainy Days)")

clear_map <- leaflet(map_data_trips |> filter(is_rainy_day == FALSE)) |>
  addProviderTiles("CartoDB.Positron") |>
  addPolygons(
    fillColor = ~pal_trips(total_trips),
    color = "white",
    weight = 1,
    fillOpacity = 0.85,
    popup = ~paste0(
      "<b>", borough, "</b><br>",
      "Clear Day<br>",
      "Total Trips: ", formatC(total_trips, format = "d", big.mark = ",")
    )
  ) |>
  addLegend("bottomright", pal = pal_trips, values = ~total_trips,
            title = "Total Trips (Clear Days)")


leafsync::sync(rainy_map, clear_map)
```

Аналіз показав, що найбільша кількість поїздок таксі припадає на Мангеттен.

По-перше, Мангеттен є діловим і туристичним центром Нью-Йорка, де розташовані офіси, готелі, театри, ресторани та основні визначні місця.
Щоденна мобільність тут значно вища, ніж у житлових районах — більшість поїздок короткі (2–3 км), тому обсяг таксі-активності завжди великий.


По-друге, інші райони міста — Бруклін, Квінз, Бронкс, Стейтен-Айленд — мають іншу транспортну поведінку.
Жителі цих районів частіше користуються приватними авто, метро чи сервісами на кшталт Uber або Lyft, які не повністю відображені в даних про жовті таксі.
Тому погодні коливання впливають там значно 


**🚕 Динаміка поїздок протягом місяця**

На цьому графіку сині стовпці відображають обсяг опадів, а помаранчева лінія — загальну кількість поїздок жовтих таксі того ж дня.

```{r}
taxi <- read_parquet("/Users/ffff/Desktop/Project/yellow_tripdata_2023-09.parquet") |>
  mutate(
    pickup_datetime = ymd_hms(tpep_pickup_datetime),
    dropoff_datetime = ymd_hms(tpep_dropoff_datetime),
    trip_duration = as.numeric(difftime(dropoff_datetime, pickup_datetime, units = "mins")),
    date = as.Date(pickup_datetime)
  ) |>
  select(PULocationID, trip_duration, date)

zones <- read.csv("/Users/ffff/Desktop/Project/taxi_zone_lookup.csv")

taxi <- taxi |>
  left_join(zones |> select(LocationID, Borough), by = c("PULocationID" = "LocationID")) |>
  rename(borough = Borough)

url <- "https://archive-api.open-meteo.com/v1/archive?latitude=40.7128&longitude=-74.0060&start_date=2023-09-01&end_date=2023-09-30&daily=precipitation_sum,temperature_2m_max,temperature_2m_min&timezone=America/New_York"

response <- GET(url)
weather_content <- content(response, "text", encoding = "UTF-8")
weather_data_json <- fromJSON(weather_content)

weather_df <- as_tibble(weather_data_json$daily) |>
  mutate(date = as_date(time)) |>
  select(date, precipitation_sum)

daily_taxi_agg <- taxi |>
  group_by(date) |>
  summarise(total_trips = n(), .groups = 'drop')

merged_df <- inner_join(daily_taxi_agg, weather_df, by = "date") |>
  mutate(day_of_month = day(date))

scaling_factor <- max(merged_df$total_trips) / max(merged_df$precipitation_sum)

p <- ggplot(merged_df, aes(x = day_of_month)) +
  geom_col(aes(y = precipitation_sum), fill = "#0072B2", alpha = 0.6) +
  geom_line(aes(y = total_trips / scaling_factor), color = "#D55E00", size = 1.1) +
  geom_point(aes(y = total_trips / scaling_factor), color = "#D55E00", size = 2) +
  scale_y_continuous(
    name = "Сума опадів (мм)",
    sec.axis = sec_axis(~ . * scaling_factor, name = "Загальна кількість поїздок")
  ) +
  labs(
    title = "Вплив опадів на кількість поїздок на таксі",
    subtitle = "Стовпці — опади (мм), Лінія — кількість поїздок",
    x = "День місяця"
  ) +
  theme_minimal() +
  theme(
    plot.title = element_text(size = 15, face = "bold"),
    plot.subtitle = element_text(size = 10),
    axis.title.y.left = element_text(color = "#0072B2", size = 12),
    axis.title.y.right = element_text(color = "#D55E00", size = 12)
  )
p
```
Під час дощу попит на таксі в центрі міста різко зростає, адже пішоходи, офісні працівники та туристи намагаються уникнути ходьби або очікування громадського транспорту.
Водночас виникають затори та дефіцит вільних автомобілів, через що кількість завершених поїздок зменшується, навіть попри високий попит.
Це створює статистичний ефект: у даних видно спад поїздок, хоча насправді це результат перевантаження, а не зниження інтересу до таксі.

**Висновок для рішення:**
Міські сервіси перевезень можуть прогнозувати пікові навантаження у дощові дні та оптимізувати розподіл таксі між районами, щоб зменшити дисбаланс між попитом і пропозицією.


**🗺️ Карта поїздок і густини населення**

```{r, message=FALSE, warning=FALSE}
zones <- st_read("/Users/ffff/Desktop/Project/taxi_zones/taxi_zones.shp")

taxi_clean <- read_parquet("/Users/ffff/Desktop/Project/yellow_tripdata_2023-09.parquet") |> 
  select(tpep_pickup_datetime, PULocationID, trip_distance, fare_amount, tip_amount, total_amount) |> 
  mutate(pickup_datetime = ymd_hms(tpep_pickup_datetime)) |> 
  filter(trip_distance > 0)

zone_counts <- taxi_clean |> 
  group_by(PULocationID) |> 
  summarise(trips = n(), .groups = "drop")

zones_joined <- zones |> 
  left_join(zone_counts, by = c("LocationID" = "PULocationID")) |> 
  mutate(trips = ifelse(is.na(trips), 0, trips))

population_df <- read_csv("/Users/ffff/Desktop/Project/top_15_nyc.csv") |> 
  clean_names()

zones_filtered <- zones_joined |> 
  filter(zone %in% population_df$nta_name)

centroids <- st_centroid(zones_filtered) |> 
  left_join(population_df, by = c("zone" = "nta_name"))

ggplot() +
  annotation_map_tile(type = "cartolight", zoom = 11) +
  geom_sf(data = zones_joined, aes(fill = trips), color = "black", size = 0.1, alpha = 0.7) +
  geom_sf(data = centroids, aes(size = population), color = "red", alpha = 0.6) +
  scale_fill_viridis_c(option = "viridis", name = "Поїздки",
                       labels = label_number(scale = 1e-3, suffix = "k")) +
  scale_size_continuous(name = "Густина населення",
                        labels = label_number(scale = 1e-3, suffix = "k")) +
  labs(title = "Карта поїздок і густини населення (вересень 2023)") +
  theme_void()

```
- Щодо теплової карти поїздок, то найбільше поїздок у центрах активності міста. На карті видно, що Манхеттен має значно більшу кількість поїздок, що підкреслює роль цих зон як центрів ділової та соціальної активності. Тут і туристичні місця, і бізнес центри, і парки, тому люди завжди викликають таксі на Манхеттен. Ще яскраві, віддалені зони на карті відповідають аеропортам, що логічно пояснюється тим, що пасажири часто користуються таксі після прильоту, щоб не думати про метро та інший громадський транспорт.<br><br>
- Помітно, що висока густина населення спостерігається не в центрі, а у спальних районах міста. Ці зони позначені великими червоними колами, бачимо,що кількість поїздок там значно менша, ніж на Манхеттені. Це свідчить про те, що мешканці переважно проживають у цих районах, але працюють чи відпочивають у центральних частинах міста, що створює типовий для мегаполісів маятниковий рух.